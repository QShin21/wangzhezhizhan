# Sources and attribution

本扩展依据《2026 三国杀王者之战神王战·广州站｜三国杀王战八人身份赛事手册》的“武将明细”制作。

技能实现、武将图片和语音参考或取自用户指定的 Qsgs-Fans 新月杀公开仓库：`shzl`、`yj`、`mobile`、`sp`、`tenyear`、`ol`、`standard_ex`、`mougong`、`freekill-core`、`utility`、`lunar`、`offline`、`xinhanwujiang`、`qsgs`、`tuguo`。

因 Gitee 的 `offline`、`tuguo` 在制作时返回 403，使用其公开 GitHub 镜像 `TamQuocSat-LangKhach/offline`、`TamQuocSat-LangKhach/tuguo`。赛事技能还引用同组织公开的 `gamemode` 与 `overseas` 包；具体到文件的映射见 `data/skill_sources.json` 和 `data/asset_sources.json`。

为使扩展可独立运行，所需技能实现和辅助模块已于 2026-06-21 收录到本仓库，并统一改为 `wzzz_v__` 命名空间。逐文件来源、原技能 ID和修改后 ID见 `data/vendor_manifest.json`；这些文件属于基于原实现的修改版本。

各参考仓库的作者、画师、配音及原始许可声明继续适用于对应内容。
